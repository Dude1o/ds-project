# 6-Minute Dashboard Presentation Script (Technical Depth Cut)
## Distributed Systems E-Commerce Simulator

**Total runtime:** 5:55 (355 seconds) — 5-second buffer under 6:00
**Pacing:** ~140 wpm, academic-technical tone. Every section has one **depth moment** marked `🔬` — a specific implementation detail that proves you understand the code, not just the concept.
**Setup before recording:**
1. App running at `http://localhost:5173/`, start on `/`
2. Open **DevTools → Console** and keep it docked at the bottom (you'll run live snippets in three sections)
3. All monitor panels reset to defaults, browser zoom ~110%, dark theme, console errors silenced

---

| Time | Section | What to show | What to say |
|------|---------|---------------|-------------|
| **0:00–0:45** | **Live E-Commerce Demo (user-facing pages)** | Navigate to **`/shop`**. Hover a ProductCard — point at the live *"Served by server-X · via consistentHash · shard-Y · Nms"* badge. Click **Order** on one product (watch the toast). Navigate to **`/orders`**. Place a real order via the OrderForm (quantity 2). Point at the new CONFIRMED row's `retryCount` badge and the `serverId`. | "Before the technical walkthrough, here's the actual e-commerce simulation these distributed systems power. Each product card carries a live badge — which server served it, via which load-balancing strategy, which shard it lives on, and the observed latency. When I place an order it flows through the same stack: routed by least-connections, wrapped in a circuit breaker, retried with exponential backoff on failure, and durably logged to a write-ahead log before the in-memory state mutates — that's why the order row shows a serving node and a retry count. Everything we're about to inspect on the monitor page is what's actually happening behind these user-facing screens, not a separate demo." |
| **0:45–0:52** | **Transition to Monitor** | Click the **Navbar → Monitor** link. Land on the monitor page. Hover over the 5 tabs. | "Now let's open the system monitor and inspect each algorithm in isolation." |
| **0:52–1:50** | **Load Balancing (7 strategies)** | Click **Load Balancer** tab. Switch strategies: `roundRobin` → `weightedRoundRobin` → `consistentHash` → `leastConnections` → `latencyBased` (5 live demos). After each switch, click **"Send 10 Requests"** and watch counter bars redistribute. On `weightedRoundRobin`, point at server-1 weight=5 vs server-3 weight=2 — bars proportional. On `consistentHash`, send two bursts with the same request key — same server each time. On `latencyBased`, point at the **EWMA latency estimates column** updating after the burst.<br><br>🔬 **Depth moment** — on the latency view, point at one EWMA number and trace: | "Seven strategies share one interface. Round-robin distributes evenly. Weighted round-robin expands each node into a number of slots equal to its weight, so higher-capacity nodes receive proportionally more traffic. Consistent hashing maps a key onto a 32-bit hash ring with a hundred and fifty virtual nodes per real node — the same key always lands on the same server. Least connections routes to the node with the fewest in-flight requests, adapting to live load. Latency-based routing maintains an exponentially-weighted moving average of measured latency per node and picks the lowest estimate. Two more live in the codebase — sticky session pins a session identifier to a node, and join-idle-queue models a worker pool where idle servers wait in a FIFO queue.<br><br>🔬 *Depth moment — latency EWMA:* the formula is new estimate equals alpha times the measured value plus one-minus-alpha times the previous estimate, with alpha equal to zero point three. So each probe shifts the estimate thirty percent of the way toward the freshly measured value and keeps seventy percent of history — that's why the numbers glide instead of jumping when I send a burst." |
| **1:50–2:18** | **Circuit Breaker** | Click **Circuit Breaker** tab. Click **"Trigger Failures"** on the Order breaker 5 times rapidly. Watch the **failure counter increment: 1, 2, 3, 4, 5** — on the 5th, point at it and say "failure counter equals threshold" as the badge flips `CLOSED` (green) → `OPEN` (red). Point at the transition timeline row's reason. Wait ~7 seconds — the badge flips to `HALF_OPEN` (amber). Click once for a successful probe (successes: 1), then again (successes: 2) — returns to `CLOSED`.<br><br>🔬 **Depth moment** — point at the exact `failures=5` and `successThreshold=2` numbers in the snapshot: | "The circuit breaker is a three-state finite-state machine. After five consecutive failures for the order service — note the failure counter climbing one, two, three, four, five — the fifth failure makes failures greater-than-or-equal to the threshold, and the breaker trips open. In open it rejects requests immediately, fail-fast, to protect downstream services. After a cool-down timeout it transitions to half-open and allows exactly one trial probe — single-probe gating is enforced by an in-flight flag. Once two probes succeed it closes again and traffic resumes.<br><br>🔬 *Depth moment:* the threshold math is failures greater-than-or-equal five trips open, and successes greater-than-or-equal two in half-open closes — both use greater-than-or-equal so the breaker trips on the Nth failure, not the N-plus-first. The transition function itself is pure: it takes the current state and counters and returns the new state with no side effects, so it's testable in isolation from the wrapper." |
| **2:18–2:38** | **Retry with Exponential Backoff** | Stay on the Circuit Breaker tab. Point at the "attempts" counter on a recent failed request row in the timeline. Switch to the **Load Balancer** tab, click "Send 10 Requests" and point at the per-request "attempts" badge — most show 1, some show 2 or 3 due to the 20% order failure injection.<br><br>🔬 **Depth moment** — open the DevTools console and run the actual backoff calculation:  | "Every service call is wrapped in retry with exponential backoff and full jitter. The wait before each attempt is `base Delay times two to the power of attempt`, capped at a maximum, then multiplied by a random number between zero and one — the full-jitter pattern that decorrelates competing clients. The load balancer's pick runs *inside* the retry closure, so each retry attempt can route to a different server, giving real fault isolation across the cluster.<br><br>🔬 *Depth moment — live math:* in the console I'll compute the delay for the order service's second retry. Config: base two hundred fifty milliseconds, max two thousand, jitter true. *(type)* `Math.min(2000, 250 * Math.pow(2, 1)) * Math.random()` → say "two hundred fifty times two to the first is five hundred milliseconds, capped under the two-thousand max, then multiplied by random gives a value between zero and five hundred — that's the actual delay slot the second retry will sleep." |
| **2:38–3:08** | **Consistent Hashing** | Stay on Load Balancer tab, `consistentHash` strategy. Send 10 requests with a fixed key — point at the determinism (same server). Click **take down** on server-3 — point that only server-3's keys redistributed, while server-1, 2, 4, 5 keep their existing keys. Bring server-3 back.<br><br>🔬 **Depth moment** — open DevTools console, run the actual djb2 hash for a key and trace it to the ring:  | "Consistent hashing places each node on a 32-bit hash ring at a hundred and fifty virtual node positions, which spreads ownership evenly. A request key is hashed and routed to the nearest clockwise position. The key property is minimal redistribution: when a node joins or leaves, only the keys that mapped to its arc move — not the whole keyspace.<br><br>🔬 *Depth moment — live hash:* the hash function is djb2. *(type in console)* `s => { let h=5381; for (const c of s) h = ((h << 5) + h) + c.charCodeAt(0); return h >>> 0; }` then call it with `'prod-1'` → say "starting seed five-three-eight-one, multiply by thirty-three and add each char code, then the unsigned-shift-zero converts the signed intermediate to a 32-bit unsigned integer. That number is binary-searched against the sorted array of fifteen-hundred ring positions — a hundred and fifty per node times five nodes — to find the first position greater-than-or-equal to the hash, with wrap-around to position zero if the hash exceeds all positions." |
| **3:08–3:55** | **Sharding (3 strategies)** | Click **Sharding** tab. Three columns: Hash, Range, Directory.<br>**Hash:** type `prod-1`, `prod-2`, `prod-17` — spread across `shard-0/1/2` via `hashCode % 3`.<br>**Range:** type `5`, `50`, `500` → fall into `shard-A` [0–999]. Type `1500` → `shard-B` [1000–1999].<br>**Directory:** use the editable lookup table to re-assign `user:1001` from `shard-0` to `shard-2` and show it sticks.<br><br>🔬 **Depth moment** — type an out-of-range value (e.g. `50000`) in the Range input and press assign; observe nothing appears in the list (the panel silently swallows it). Then in devtools verify the throw:  | "Three sharding strategies live side by side. Hash sharding computes hash of key modulo shard count — deterministic, even spread, but adding a shard reshuffles nearly every key. Range sharding maps contiguous numeric ranges to shards — efficient for range queries but prone to hotspots at the high end. Directory sharding uses an explicit lookup table, giving full placement flexibility at the cost of a routing hop on every access.<br><br>🔬 *Depth moment — boundary handling:* watch what happens when I type fifty thousand into the range input. Nothing appears in the list — the panel's assign handler has a try-catch that silently swallows the error. Under the hood `getRangeShard` throws 'no partition covers value', because the configured partitions only go up to nine thousand nine hundred ninety-nine. There is **no default route** for out-of-range keys — which is a deliberate design choice that surfaces range-sharding's weakness: you must provision partitions for your entire keyspace up front, or accept silent drops at the edges." |
| **3:55–4:45** | **Heartbeat Monitoring** | Click **Heartbeat** tab. Six nodes pulsing green (ALIVE). Click **"Crash"** on node-3 — its pulse ring stops. **Count out loud** in sync with ticks: "tick… missed-beats one, still ALIVE… tick… missed-beats two, still ALIVE… tick… missed-beats three — crossing suspect threshold — badge flips to SUSPECTED (amber)… tick… missed-beats four… tick… five… tick… missed-beats six — crossing dead threshold — DEAD (red)." Point at the transition timeline rows: ALIVE→SUSPECTED→DEAD with timestamps. Click **"Restore"** — returns to ALIVE.<br><br>🔬 **Depth moment** — point at the `missedBeats` number in the registry entry and state the formula: | "Failure detection is a periodic heartbeat protocol. Each node emits a beat; the monitor computes missed-beats as floor of elapsed time divided by interval. Crossing the suspect threshold transitions the node to suspected, and crossing the dead threshold marks it dead. Crucially, only explicitly crashed nodes stop auto-renewing their heartbeat; healthy nodes refresh on every tick, so the detector watches for the *absence* of beats, not their presence.<br><br>🔬 *Depth moment — counting beats:* the interval is one thousand milliseconds, suspect threshold is three, dead threshold is six. So missed-beats equals floor of elapsed-over-one-thousand. At three seconds elapsed the floor is three, greater-than-or-equal three trips SUSPECTED. At six seconds elapsed the floor is six, greater-than-or-equal six trips DEAD. The two-phase progression gives operators a window to investigate before declaring a node permanently gone." |
| **4:45–5:50** | **Replication — Active + Passive + WAL** | Click **Replication** tab. PRIMARY on the left, 3 REPLICAs on the right, animated WAL stream in the centre.<br>**Active mode** (default): click **"Write Record"** 3 times — point at the **LSN column incrementing 1, 2, 3** as each entry appends; then watch all 3 replicas' `lastLSN` advance to the same value simultaneously, and the WAL entry flips to "applied" only after all confirm.<br>**Toggle to Passive**: click "Write Record" 5 times rapidly — replicas fall behind at different rates (lag 350/700/1100 ms); point at per-replica lag badges and entries staying "unflushed" until the slowest replica catches up.<br><br>🔬 **Depth moment** — point at the LSN sequence and the applied flag transition: | "Replication comes in two flavours around a shared write-ahead log. The WAL is an append-only log of mutations with monotonically increasing log sequence numbers; every mutation is logged *before* it is applied, which is real write-ahead-log semantics. In active replication a write is applied synchronously to all replicas via Promise.all — the write only commits when the slowest replica acknowledges. This is strong consistency at the cost of write latency. In passive replication the primary commits locally and replicas pull new entries on a polling interval, each with its own simulated lag — this is eventual consistency with observable replica divergence.<br><br>🔬 *Depth moment — why LSNs must be monotonic:* look at the LSN column — one, two, three, never gaps, never decreases. Monotonicity is what lets `getSince(last LSN)` return exactly the entries a replica is missing without missing any or replaying any. The applied flag on each entry starts false; in active mode it flips true only after the `Promise.all` over replicas resolves, and in passive mode it flips true only when the *minimum* `last LSN` across replicas catches up past that entry — that's the all-replicas-caught-up gate that marks the write durably replicated." |
| **5:50–5:55** | **Closing** | Slowly scroll across all five monitor tabs in 3 seconds. | "That covers the live store, seven load balancers, the circuit breaker with its threshold math, retry with full-jitter backoff, consistent hashing with the live djb2 ring lookup, three sharding strategies including out-of-range handling, heartbeat detection with counted missed-beats, and active-versus-passive replication with monotonic WAL sequence numbers — all running deterministically in the browser." |

---

## DevTools Console Snippets (copy-paste ready)

```js
// Consistent Hashing depth moment — djb2 hash for a key
(s => { let h=5381; for (const c of s) h = ((h << 5) + h) + c.charCodeAt(0); return h >>> 0; })('prod-1')
// Sample output: 2218477107 — this is what binary-searched against the 150-vnode ring

// Retry depth moment — order service 2nd-attempt backoff with full jitter
Math.min(2000, 250 * Math.pow(2, 1)) * Math.random()
// Returns a value in [0, 500) — the actual sleep slot before attempt 2

// Sharding depth moment — out-of-range range-shard lookup throws (simulated)
// (the real function is not on window; this mirrors the logic)
function rangeShard(v, parts) { for (const p of parts) if (v >= p.min && v <= p.max) return p.shardId; throw new Error(`no partition covers ${v}`); }
rangeShard(50000, [{shardId:'shard-A',min:0,max:999},{shardId:'shard-B',min:1000,max:1999},{shardId:'shard-C',min:2000,max:9999}])
// throws: "no partition covers 50000"
```

---

## Delivery Notes

- **Don't read verbatim.** Use the "What to say" column as a skeleton; paraphrase naturally.
- **The `🔬` depth moments are the point of this cut.** Practice each one in isolation before the full run — they are the signal to the professor that you understand implementation, not just concept.
- **DevTools snippets:** have the three snippets pre-typed in a notepad window; paste them into the console at the right moment rather than typing live. Typing live costs ~10s per snippet and risks typos under pressure.
- **Hands busy, mouth steady.** Aim to finish clicking within the first 5 seconds of each section so the visual lines up with narration.
- **If you drift over time**, trim Load Balancing first — drop the weighted-round-robin live demo (mention it verbally only). Cutting one strategy switch saves ~8 seconds. Second trim: compress Replication to one active + two passive writes (saves ~15 seconds).
- **If you drift under time**, slow down on the Circuit Breaker transition timeline and read out two full transition rows with their timestamps.
- **Recording tips:** 1080p, 30fps, OBS or Loom; disable notifications; close other tabs; keep the browser console docked at the bottom so it's visible alongside the dashboard.
- **Practice run:** Do one full dry-run with a stopwatch before recording. The script is calibrated to ~140 wpm across 355 seconds.

---

## Timing Summary

| Block | Duration | Cumulative |
|-------|----------|------------|
| Live E-Commerce Demo (/shop + /orders) | 0:45 | 0:45 |
| Transition to Monitor | 0:07 | 0:52 |
| Load Balancing (7 strategies) + EWMA depth | 0:58 | 1:50 |
| Circuit Breaker + threshold-math depth | 0:28 | 2:18 |
| Retry + live backoff-math depth | 0:20 | 2:38 |
| Consistent Hashing + live djb2 depth | 0:30 | 3:08 |
| Sharding + out-of-range throw depth | 0:47 | 3:55 |
| Heartbeat + counted missed-beats depth | 0:50 | 4:45 |
| Replication + WAL + monotonic-LSN depth | 1:05 | 5:50 |
| Closing | 0:05 | 5:55 |
| **Buffer under 6:00** | | **5 sec** |